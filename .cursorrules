# Правила обновления и деплоя проекта Riderra

## 🚀 Автоматическое обновление на сервере

### Когда пользователь просит обновить проект на сервере:

1. **Используйте скрипт `update.sh`** - он автоматически выполняет все необходимые шаги:
   ```bash
   ssh root@91.229.9.217 "cd /opt/riderra && bash update.sh"
   ```

2. **Если скрипта нет на сервере**, выполните команды вручную:
   ```bash
   ssh root@91.229.9.217 "cd /opt/riderra && \
     git stash 2>/dev/null || true && \
     git pull origin master && \
     npm install && \
     npx prisma migrate deploy 2>/dev/null || true && \
     npx prisma generate && \
     rm -rf .nuxt dist node_modules/.cache && \
     npm run generate && \
     pm2 restart riderra --update-env && \
     pm2 logs riderra --lines 10"
   ```

### Стандартная последовательность обновления:

1. ✅ **Обновление кода**: `git pull origin master`
2. ✅ **Установка зависимостей**: `npm install`
3. ✅ **Миграции БД**: `npx prisma migrate deploy` (если есть)
4. ✅ **Очистка кеша**: `rm -rf .nuxt dist node_modules/.cache`
5. ✅ **Пересборка**: `npm run generate`
6. ✅ **Очистка кеша Nginx**: `rm -rf /var/cache/nginx/*` и `systemctl reload nginx`
7. ✅ **Перезапуск PM2**: `pm2 restart riderra --update-env`
8. ✅ **Проверка статуса**: `pm2 status`

### Важные моменты:

- **Всегда очищайте кеш** перед пересборкой (`rm -rf .nuxt dist node_modules/.cache`)
- **Используйте `npm run generate`** для статической генерации (не `build`)
- **PM2 перезапуск** должен быть с флагом `--update-env` для обновления переменных окружения
- **После обновления** проверяйте статус PM2 и логи

### Если изменения не видны на сайте:

1. Проверьте кеш браузера (жесткая перезагрузка: Cmd+Shift+R)
2. Очистите кеш Nginx на сервере
3. Проверьте, что PM2 перезапустился успешно
4. Проверьте логи PM2 на ошибки

### Информация о сервере:

- **IP**: 91.229.9.217
- **Пользователь**: root
- **Директория проекта**: /opt/riderra
- **PM2 процесс**: riderra
- **Порт**: 3000 (проксируется через Nginx на 443)

