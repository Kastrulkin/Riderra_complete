# 🔐 Система авторизации Riderra

## ✅ **ПОЛНОСТЬЮ РЕАЛИЗОВАНА!**

### 🚀 **Что было добавлено:**

#### **1. База данных**
- ✅ Таблица `User` для пользователей системы
- ✅ Связь между `User` и `Driver` (один к одному)
- ✅ Роли: `admin`, `driver`
- ✅ Хеширование паролей с bcrypt

#### **2. API авторизации**
- ✅ `POST /api/auth/register` - регистрация водителя
- ✅ `POST /api/auth/login` - вход в систему
- ✅ `GET /api/auth/me` - информация о текущем пользователе
- ✅ `POST /api/auth/create-admin` - создание админа

#### **3. Страницы авторизации**
- ✅ `/login` - страница входа
- ✅ `/register` - страница регистрации водителя
- ✅ Красивый дизайн в стиле сайта
- ✅ Валидация форм
- ✅ Обработка ошибок

#### **4. Middleware защиты**
- ✅ `auth.js` - проверка авторизации
- ✅ `admin.js` - проверка роли админа
- ✅ `driver.js` - проверка роли водителя
- ✅ Автоматическое перенаправление

#### **5. Интеграция**
- ✅ Обновленная навигация с кнопками входа/выхода
- ✅ Защищенные страницы:
  - `/driver-dashboard` - только для водителей
  - `/admin-drivers` - только для админов
  - `/admin-driver-switch` - только для админов
- ✅ Vuex store для управления состоянием
- ✅ JWT токены с автоматическим обновлением

## 🎯 **Как использовать:**

### **Для администраторов:**
1. **Вход:** `/login`
   - Email: `admin@riderra.com`
   - Пароль: `admin123`
2. **Доступные страницы:**
   - `/admin-drivers` - управление водителями
   - `/admin-driver-switch` - переключение между водителями

### **Для водителей:**
1. **Регистрация:** `/register`
   - Заполните форму с данными и комиссией
   - Автоматический вход после регистрации
2. **Вход:** `/login`
   - Используйте email и пароль
3. **Доступные страницы:**
   - `/driver-dashboard` - дашборд водителя

### **Создание нового админа:**
```bash
# Запустите скрипт создания админа
node create-admin.js
```

## 🔧 **Технические детали:**

### **JWT токены:**
- Секрет: `process.env.JWT_SECRET` или `'your-secret-key'`
- Время жизни: 7 дней
- Автоматическое обновление при активности

### **Хеширование паролей:**
- Алгоритм: bcrypt
- Соль: 10 раундов
- Безопасное хранение в базе данных

### **Роли пользователей:**
- `admin` - полный доступ к админ-панели
- `driver` - доступ к дашборду водителя

### **Защита роутов:**
- Middleware автоматически проверяет авторизацию
- Перенаправление на `/login` при отсутствии токена
- Проверка ролей для доступа к страницам

## 📱 **Пользовательский интерфейс:**

### **Навигация:**
- **Неавторизованные:** кнопки "Войти" и "Регистрация"
- **Авторизованные:** email пользователя и кнопка "Выйти"

### **Страницы входа/регистрации:**
- Адаптивный дизайн
- Валидация форм
- Обработка ошибок
- Красивые анимации

## 🚀 **Готово к использованию!**

Система авторизации полностью интегрирована и готова к работе. Все страницы защищены, пользователи могут регистрироваться и входить в систему, а администраторы имеют полный контроль над платформой.

**Основные URL:**
- `/login` - вход в систему
- `/register` - регистрация водителя
- `/driver-dashboard` - дашборд водителя (требует авторизации)
- `/admin-drivers` - админ-панель (требует роль admin)
- `/admin-driver-switch` - переключение водителей (требует роль admin)
