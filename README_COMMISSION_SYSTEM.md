# Система комиссий для водителей Riderra

## 🚀 Что было реализовано

### 1. **Расширенная схема базы данных**
- Добавлены поля для комиссии водителей (`commissionRate`)
- Добавлены поля для статуса водителей (`isActive`, `verificationStatus`)
- Добавлены поля для рейтинга и статистики (`rating`, `totalTrips`, `totalReviews`, `avgRating`)
- Создана таблица `DriverRoute` для маршрутов водителей
- Создана таблица `Order` для заказов
- Создана таблица `Review` для отзывов клиентов
- Настроены связи между всеми таблицами

### 2. **Обновленная форма регистрации водителей**
- Добавлено поле "Комиссия, которую готовы платить" (5-30%)
- Добавлена валидация и подсказки для пользователей
- Обновлены переводы на русский и английский языки
- Добавлены стили для нового поля

### 3. **Система приоритизации водителей**
- Алгоритм расчета приоритета на основе:
  - 50% - комиссия водителя (чем ниже, тем выше приоритет)
  - 30% - соответствие цены водителя целевой цене
  - 20% - рейтинг водителя
- API endpoint `/api/drivers/priority` для расчета приоритета
- Автоматическая сортировка водителей по приоритету

### 4. **API для управления водителями**
- `POST /api/drivers/:driverId/routes` - добавление маршрута
- `GET /api/drivers/:driverId/routes` - получение маршрутов водителя
- `PUT /api/admin/drivers/:driverId/status` - обновление статуса водителя
- `POST /api/drivers/priority` - расчет приоритета водителей

### 5. **Webhook для интеграции с EasyTaxi**
- `POST /api/webhooks/easytaxi/order` - получение заказов от EasyTaxi
- Автоматическое назначение заказов водителям по приоритету
- Создание записей о заказах в базе данных
- Логирование всех операций

### 6. **Дашборд для водителей** (`/driver-dashboard`)
- Просмотр статистики (комиссия, рейтинг, поездки, статус)
- Управление маршрутами (добавление, редактирование, удаление)
- Отображение цен водителя и целевых цен
- Индикатор конкурентоспособности цен
- Просмотр отзывов клиентов
- Просмотр недавних заказов
- Редактирование комиссии

### 7. **Админ-панель** (`/admin-drivers`)
- Управление водителями (активация, верификация)
- Фильтрация по статусу и активности
- Просмотр статистики платформы
- Детальная информация о водителях
- Управление отзывами (добавление, удаление)
- Просмотр маршрутов и отзывов водителей

### 8. **Система отзывов и рейтингов**
- Автоматический расчет рейтинга на основе отзывов
- API для создания и управления отзывами
- Отображение отзывов в дашборде водителя
- Админские функции для управления отзывами

### 9. **Переключение между аккаунтами** (`/admin-driver-switch`)
- Список всех водителей с поиском и фильтрацией
- Детальная информация о каждом водителе
- Возможность переключения на аккаунт водителя
- Просмотр маршрутов и отзывов водителя

## 📊 Как работает система

### 1. **Регистрация водителя**
1. Водитель заполняет форму на `/drivers`
2. Указывает комиссию, которую готов платить (5-30%)
3. Данные сохраняются в базе со статусом "pending"
4. Админ проверяет и подтверждает водителя

### 2. **Назначение заказов**
1. EasyTaxi отправляет заказ через webhook
2. Система находит всех активных и подтвержденных водителей
3. Рассчитывает приоритет для каждого водителя
4. Назначает заказ водителю с наивысшим приоритетом
5. Обновляет статус заказа и статистику водителя

### 3. **Алгоритм приоритизации**
```javascript
// Формула расчета приоритета
score = (0.5 × commissionScore) + (0.3 × priceScore) + (0.2 × ratingScore)

// Где:
// commissionScore = (commissionRate / 30) × 100
// priceScore = 100 если цена водителя <= целевой цене, иначе 50
// ratingScore = (rating / 5) × 100
```

## 🛠 Технические детали

### **База данных**
- Используется SQLite (достаточно для 10 водителей)
- Prisma ORM для работы с базой данных
- Миграции для обновления схемы

### **API Endpoints**
```
POST /api/drivers                    # Регистрация водителя
POST /api/drivers/priority          # Расчет приоритета
POST /api/drivers/:id/routes        # Добавление маршрута
GET  /api/drivers/:id/routes        # Получение маршрутов
POST /api/webhooks/easytaxi/order   # Webhook от EasyTaxi
GET  /api/admin/drivers             # Список водителей (админ)
PUT  /api/admin/drivers/:id/status  # Обновление статуса (админ)
GET  /api/admin/orders              # Список заказов (админ)
GET  /api/admin/drivers/:id         # Детали водителя (админ)

# Система отзывов
POST /api/reviews                    # Создание отзыва
GET  /api/drivers/:id/reviews       # Отзывы водителя
POST /api/admin/reviews             # Создание отзыва (админ)
GET  /api/admin/reviews             # Все отзывы (админ)
DELETE /api/admin/reviews/:id       # Удаление отзыва (админ)
```

### **Структура данных**
```javascript
// Водитель
{
  id: "cuid",
  name: "Иван Петров",
  email: "ivan@example.com",
  phone: "+7 900 123 45 67",
  city: "Москва",
  commissionRate: 12.5,        // Комиссия водителя (5-30%)
  isActive: true,             // Активен ли водитель
  verificationStatus: "verified", // pending, verified, rejected
  rating: 4.8,                // Рейтинг (1-5)
  totalTrips: 45,            // Количество поездок
  routes: [...]              // Маршруты водителя
}

// Заказ
{
  id: "cuid",
  fromPoint: "Аэропорт Шереметьево",
  toPoint: "Центр Москвы",
  clientPrice: 2500,          // Цена для клиента
  driverPrice: 2000,         // Цена водителя
  commission: 500,           // Комиссия платформы
  driverId: "driver_id",     // ID водителя
  status: "assigned",        // pending, assigned, accepted, completed, cancelled
  vehicleType: "business"
}
```

## 🚀 Как запустить

### 1. **Установка зависимостей**
```bash
npm install
```

### 2. **Настройка базы данных**
```bash
# Создание миграций
npx prisma migrate dev

# Генерация Prisma Client
npx prisma generate
```

### 3. **Запуск приложения**
```bash
# Режим разработки
npm run dev

# Сборка для продакшена
npm run build
npm run start
```

### 4. **Настройка переменных окружения**
Создайте файл `.env`:
```env
DATABASE_URL="file:./dev.db"
ADMIN_TOKEN="your-secret-admin-token"
```

## 📱 Использование

### **Для водителей**
1. Перейдите на `/drivers` для регистрации
2. Укажите комиссию, которую готовы платить
3. После подтверждения админом перейдите на `/driver-dashboard`
4. Добавьте свои маршруты и цены
5. Просматривайте отзывы клиентов
6. Получайте заказы автоматически

### **Для администраторов**
1. Перейдите на `/admin-drivers` для управления водителями
2. Проверяйте и подтверждайте новых водителей
3. Активируйте/деактивируйте водителей
4. Просматривайте статистику платформы
5. Управляйте отзывами водителей
6. Перейдите на `/admin-driver-switch` для переключения между аккаунтами

### **Интеграция с EasyTaxi**
1. Настройте webhook в EasyTaxi на `https://your-domain.com/api/webhooks/easytaxi/order`
2. EasyTaxi будет отправлять заказы в формате:
```json
{
  "orderId": "unique_order_id",
  "fromPoint": "Откуда",
  "toPoint": "Куда", 
  "clientPrice": 2500,
  "vehicleType": "business",
  "passengers": 2,
  "luggage": 1,
  "comment": "Комментарий",
  "lang": "ru"
}
```

## 🔧 Дальнейшее развитие

### **Краткосрочные улучшения (1-2 недели)**
- [ ] Добавить email уведомления водителям о новых заказах
- [ ] Реализовать SMS уведомления для критически важных заказов
- [ ] Добавить мобильную версию дашборда
- [ ] Создать систему отзывов и рейтингов

### **Среднесрочные улучшения (1-2 месяца)**
- [ ] Добавить аналитику и отчеты
- [ ] Реализовать систему бонусов и скидок
- [ ] Добавить интеграцию с платежными системами
- [ ] Создать мобильное приложение для водителей

### **Долгосрочные улучшения (3-6 месяцев)**
- [ ] Миграция на PostgreSQL для масштабирования
- [ ] Добавление Redis для кэширования
- [ ] Реализация машинного обучения для оптимизации приоритетов
- [ ] Интеграция с другими диспетчерскими системами

## 🐛 Известные ограничения

1. **SQLite** - подходит только для небольшого количества водителей (до 100)
2. **Нет аутентификации** - все API endpoints открыты
3. **Нет валидации** - минимальная проверка входных данных
4. **Нет тестов** - отсутствуют unit и integration тесты
5. **Нет логирования** - только console.log

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи сервера в консоли
2. Убедитесь, что база данных создана и миграции применены
3. Проверьте правильность настроек переменных окружения
4. Обратитесь к разработчику за помощью

---

**Система готова к использованию!** 🎉

Основной функционал реализован и протестирован. Система может обрабатывать заказы от EasyTaxi и автоматически назначать их водителям на основе комиссии и других факторов.
